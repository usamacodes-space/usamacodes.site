# Optional: for AI chat (Groq). Without it, the site uses rule-based fallback answers.
# Local dev (vite-plugin-api): add to .env.local
# Vercel: set in Project Settings → Environment Variables (Production + Preview)
GROQ_API_KEY=your_groq_api_key_here
